export const grades = ['Weak confirmation', 'Self-reported', 'Counterparty-confirmed', 'Independently evidenced'];
export function weakest(records) { return records.length ? grades[Math.min(...records.map(t => grades.indexOf(t.grade)))] : 'No records'; }
export function balances(records) { return records.reduce((a,t) => {a[t.currency] += (t.direction === 'in' ? 1 : -1)*Math.round(t.amount*100);return a;}, {USD:0, ZiG:0}); }
export function replacementMargin({revenue, currency, rate, replacement}) { if (![revenue,rate,replacement].every(Number.isFinite)||revenue<0||rate<=0||replacement<0) throw new Error('Enter valid positive amounts and a rate above zero.'); const usd = currency === 'USD' ? revenue : revenue/rate; return {proceeds:usd, profit:usd-replacement, percent:usd ? (usd-replacement)/usd*100 : 0}; }
export function passport(records) { const sales = records.filter(t=>t.category==='Sales'); return {business:'Mbare Fresh Market', generatedAt:new Date().toISOString(), demonstration:true, balancesMinorUnits:balances(records), evidenceGrade:weakest(records), salesEvidence: Object.fromEntries(['USD','ZiG'].map(c=>{const s=sales.filter(t=>t.currency===c);const total=s.reduce((a,t)=>a+t.amount,0);return [c,{total,confirmed:s.filter(t=>grades.indexOf(t.grade)>=2).reduce((a,t)=>a+t.amount,0)}]})), records, limitations:['Fictional demonstration data. Not a credit score or lending recommendation.','Anchor records evidence purchases and payments, not profitability.','USD and ZiG are reported separately. No live exchange-rate feed.']}; }
export function networkView(records) { return records.filter(r=>r.source==='anchor').map(({business,region,ordersPrevious,ordersCurrent,daysPrevious,daysCurrent,exposureUSD})=>({business,region,ordersPrevious,ordersCurrent,daysPrevious,daysCurrent,exposureUSD,signal:daysCurrent-daysPrevious>=7?'Needs attention':ordersCurrent<ordersPrevious*.8?'Possible constraint':'Stable'})); }
